# Claude Instructions

The Appverse programme is governed by Mission Control in `oasisbaklawa2006/Oasis-Baklawa-Central`. Preserve the assigned ASM work item, repository boundary, dependencies, and stop condition before material work.

Core owns the canonical shared backend and all Appverse Supabase migration SQL. Do not absorb Central UI, Buyer App, AI Studio, Trace, or another workstream into a Core mission without explicit Mission Control reassignment.

For mismatched pasted instructions start with `ROUTING REJECTED — instruction does not belong to this thread.`, identify the likely ASM route when possible, state `No code, PR, migration, deployment, or scope expansion performed.`, and stop.

If an upstream dependency is missing, report `BLOCKED` and stop safely. Do not fabricate schema/data/identifiers, bypass governance, apply manual production SQL, rewrite migration history, or create parallel backend authority.

`PR MERGED != STAGE CLEARED`. The canonical ASM is maintained in `oasisbaklawa2006/Oasis-Baklawa-Central`.
